// Image types live in @/lib/types-api — re-export from there for path compatibility.
export type {
  ImageTask,
  ImageTaskParams,
  SubmitTaskRequest,
  SubmitTaskResponse,
  EditTaskRequest,
} from "@/lib/types-api";
